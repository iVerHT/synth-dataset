paths:
  shards_root: output/shards
  merged_dir: output/merged
  dataset_dir: output/dataset

generate:
  shards: 2
  images_per_shard: 10
  seed: 0
  max_parallel: 2
  negative_prob: 0.15

split:
  val_ratio: 0.2
  seed: 42

steps:
  - generate
  - merge
  - split